export interface ILimitReachedStock {
  change: string;
  changePercent: string;
  high: string;
  latestTradingDay: string;
  low: string;
  open: string;
  previousClose: string;
  price: string;
  symbol: string;
  volume: string;
}